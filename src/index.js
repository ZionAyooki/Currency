import React from 'react';
import ReactDOM from 'react-dom';
import CurrencyApp from './app/CurrencyApp';

ReactDOM.render(
  <React.StrictMode>
    <CurrencyApp />
  </React.StrictMode>,
  document.getElementById('root')
);
